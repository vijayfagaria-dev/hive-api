# hive-web production .env  →  copy to /opt/hive/hive-web/.env
# In prod Caddy routes /api → FastAPI directly, so this is mostly a safety net
# for any same-origin proxying. Point it at the local backend.
API_ORIGIN=http://127.0.0.1:8000
